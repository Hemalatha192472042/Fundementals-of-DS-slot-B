import pandas as pd

df = pd.read_csv("Problem08_Scores.csv")

print("Mean =", df["Value"].mean())
print("Standard Deviation =", df["Value"].std())

se = df["Value"].std() / (len(df) ** 0.5)

print("Standard Error =", se)
