import pandas as pd

df = pd.read_csv("Problem13_CallCentre.csv")

mean = df["Mean"][0]
sd = df["SD"][0]
n = df["Sample_Size"][0]

se = sd / (n**0.5)
margin = 1.96 * se

print("95% Confidence Interval =", (mean-margin, mean+margin))
